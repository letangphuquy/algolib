#include<bits/stdc++.h>
using namespace std;

int main() {
	freopen("problem.tex", "r", stdin);
	freopen("problem.md", "w", stdout);
	string line;
	string text;
	while (getline(cin,line)) {
// 		for (auto& c : line) if (c == '$') c = '~';
// 		cout << line << '\n';
		text += line;
	}
}
